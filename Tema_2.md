# Тема 2. Базовые операции языка Python
Отчет по Теме #2 выполнил(а):
- Пинегин Дмитрий Александрович
- ПИЭ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | + |
| Задание 7 | + | + |
| Задание 8 | + | + |
| Задание 9 | + | + |
| Задание 10 | + | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Выведите в консоль три строки. Первая – любое число. Вторая – любое число в виде строки. Третья – любое число с плавающей точкой.

```python
print(23423)
print("23423")
print(23.423)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_1.png)

## Выводы

В данном коде выводятся три строки с использованием функции `print()`. Каждая строка содержит разные значения:

1. `print(23423)`: Выводится целое число 23423. Это число не взаимодействует со строковыми операциями и выводится как есть.

2. `print('234233')`: Выводится строка '23423', так как она заключена в одинарные кавычки. В этом случае это текстовая строка, а не число.

3. `print(1.23)`: Выводится число с плавающей точкой 23.423. Так же, как и в первом случае, оно выводится как числовое значение.

## Лабораторная работа №2
### Выведите в консоль три строки. Первая - результат сложения или вычитания минимум двух переменных типа int. Вторая - результат сложения или вычитания минимум двух переменных типа float. Третья - результат сложения или вычитания минимум двух переменных типа int и float.

```python
print(23 - 42)
print(2.3 + 4.1)
print(2.43 + 7)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_2.png)

## Выводы

В данном коде выводятся три строки с использованием функции `print()`. Каждая строка содержит разные значения:

1. `print(23 - 42)`: Выводится целое число -19. Это число является результатом сложения двух переменных типа int.

2. `print(2.3 + 4.1)`: Выводится число с плавающей точкой 6.3999999999999995. Это число является результатом сложения двух переменных типа float.

3. `print(2.43 + 7)`: Выводится целое число 9.43. Это число является результатом сложения двух переменных типа int и float.

## Лабораторная работа №3
### Выведите в консоль три строки. Первая - обычная строка. Вторая - F строка с использованием заранее объявленной переменной. Третья - сложите две или более строк в одну.

```python
print("Hellow world")

world = "Worlde"
print(f"Hello, {world}!")

one = "hi "
two = "worl"
print(one + two)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_3.png)

## Выводы

В данном коде выводятся три строки с использованием функции `print()` и различных переменных. Каждая строка содержит разные значения:

1. `print("Hellow world")`: Выводится строковое значение 'Hellow world'.

2. `print(f"Hello, {world}!")`: Выводится строка 'Hello, Worlde!', с переменной world заданой со строковым значением 'Worlde".

3. `print(one + two)`: Выводится строковое значение 'hi worl', при переменных 'hi' и 'worl'. Это показывает как складываются строковые переменные.
  
## Лабораторная работа №4
### Выведите в консоль три строки. Первая - трансформация любого типа переменной в bool. Вторая - трансформация любого типа переменной в float. Третья - трансформация любого типа переменной в str.

```python
one = 'hi'
print(bool(one))

two = 12
print(float(two))

three = 32
print(str(three))
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_4.png)

## Выводы

В данном коде выводятся три строки с использованием функции `print()` и различных переменных. Каждая строка содержит разные значения:

1. `print(bool(one))`: Выводится значение True, показывая трансформацию любого типа переменных в bool.

2. `print(float(two))`: Выводится значение 12.0, показывая трансформацию типа int в float.

3. `print(one + two)`: Выводится строковое значение 32, показывая трансформацию любого типа переменных в str.

## Лабораторная работа №5
### Присвойте трем переменным различные значения, воспользовавшись функцией input().

```python
one = input("one: ")
two = input("two: ")
three = input("three: ")

print(one, two, three)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_5.png)

## Выводы

В данном коде выводится строка с использованием функции `print()` и `input()`. значения строки задаётся следующим образом:

1. `one = input("one: ")`: Задается значение переменной one через консоль.

2. `two = input("two: ")`: Задается значение переменной two через консоль.

3. `three = input("three: ")`: Задается значение переменной three через консоль.

4. `print(one, two, three)`: Выводится строковое значение заданных переменных one, two, three.


## Лабораторная работа №6
### Создайте две любые числовые переменные и выполните над ними несколько математических операций: возведение в степень, обычное деление, целочисленное деление, нахождение остатка от деления. При желании вы можете проверить как работают эти вычисления с разными типами данных, например, сначала создать две переменные int, затем создать две переменные float и наконец создать переменные типа int и float и провести над ними операции, прописанные выше.

```python
a = 5
b = 4
print("Возведение в степень:", a ** b)
print("Обычное деление:", a / b)
print("Целочисленное деление:", a // b)
print("Нахождение остатка от деления:", a % b)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_6.png)

## Выводы

В данном коде выводятся четыре строки с использованием функции `print()` и математических функций. Каждая строка содержит разные значения:

1. `print("Возведение в степень:", a ** b)`: Выводится значение 625, являющееся переменной a в степени b.

2. `print("Обычное деление:", a / b)`: Выводится значение 1.25, являющееся результатом деления a на b

3. `print("Целочисленное деление:", a // b)`: Выводится значение 1, являющееся результатом целочисленного деления a на b

4. `print("Нахождение остатка от деления:", a % b)`: Выводится значение 1, являющееся остатком деления a на b

## Лабораторная работа №7
### Создайте любую строковую переменную и произведите над ней математическое действие умножение на любое число.

```python
a = 'meh'
print(a * 3)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_7.png)

## Выводы

В данном коде выводится строка с использованием функции `print()`. строка имеет следующие значения:

1. `print(a * 3)`: Выводится строковое значение 'mehmehmeh', при переменной а со значением 'meh'. При умножении сторокового значения, строка просто дублируется.


## Лабораторная работа №8
### Посчитайте сколько раз символ ‘o’ встречается в строке ‘Hello World’.

```python
a = 'Hello World!'
print(a.count('o'))
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_8.png)

## Выводы

В данном коде выводится строка с использованием функции `print()`. строка имеет следующие значения:

1. `print(a.count('o'))`: Выводится число, равное количеству найденных символов "о" в строке.


## Лабораторная работа №9
### Напишите предложение ‘Hello World’ в две строки. Написанная программа должна занимать одну строку в редакторе кода

```python
print('Hello\nWorld')
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_9.png)

## Выводы

В данном коде выводится две строки с использованием функции `print()`. строки имеет следующие значения:

1. `print('Hello\nWorld')`: Выводится две строки со значением 'Hello' и 'World'.


## Лабораторная работа №10
### Из предложения ‘Hello World’ выведите в консоль только 2 символ, а затем выведите слово ‘Hello’

```python
a = 'Hello World'
print(a[1])
print(a[:5])
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Lab2_10.png)

## Выводы

В данном коде выводятся две строки с использованием функции `print()`. Каждая строка содержит разные значения:

1. `print(a[1])`: Выводится 2ой символ строки 'Hello World'.

2. `print(f"Hello, {world}!")`: Выводится слово 'Hello' строки 'Hello World'.


## Самостоятельная работа №1
### Выведите в консоль булевую переменную False, не используя слово False в строке или изначально присвоенную булевую переменную. Программа должна занимать не более двух строк редактора кода.

```python
a = 0
print(bool(a))
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_1.png)

## Выводы

В данном коде выводятся значение bool с использованием функции `print()`:

1. `print(bool(a))`: Выводится false, т.к. значение переменной a является нулем.

  
## Самостоятельная работа №2
### Присвоить значения трем переменным и вывести их в консоль, используя только две строки редактора кода

```python
a, b, c = input(), input(), input()
print(a, b, c)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_2.png)

## Выводы

В данном коде выводятся три строки с использованием функции `print()` и `input()`. Каждая строка содержит разные значения:

1. `a, b, c = input(), input(), input()`: Задается значение переменным a, b, с через консоль с помощью одной строки.

2. `print(a, b, c)`: Выводится значение трёх переменных'.
  
## Самостоятельная работа №3
### Реализуйте ввод данных в программу, через консоль, в виде только целых чисел (тип данных int). То есть при вводе буквенных символов в консоль, программа не должна работать. Программа должна занимать не более двух строк редактора кода.

```python
a = int(input())
print(a)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_3.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и `input()`:

1. `a = int(input())`: Задается значения через консоль, которое принимает только значения типа int.

2. `print(f"Hello, {world}!")`: Выводится переменная типа int.
  
## Самостоятельная работа №4
### Создайте только одну строковую переменную. Длина строки должна не превышать 5 символов. На выходе мы должны получить строку длиной не менее 16 символов. Программа должна занимать не более двух строк редактора кода

```python
a = 'test'
print(a*4)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_4.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()`:

1. `print(a*4)`: Выводится значение строки переменной а дублированное 4 раз.


## Самостоятельная работа №5
### Создайте три переменные: день (тип данных - числовой), месяц (тип данных - строка), год (тип данных - числовой) и выведите в консоль текущую дату в формате: “Сегодня день месяц год. Всего хорошего!” используя F строку и оператор end внутри print(), в котором вы должны написать фразу “Всего хорошего!”. Программа должна занимать не более двух строк редактора кода.

```python
d, m, y = 14, 'Сентября', 2025
print(f'Сегодня {d} {m} {y} года. ', end='Всего хорошего!')
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_5.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()`:

1. `print(f'Сегодня {d} {m} {y} года. ', end='Всего хорошего!')`: Выводится строка используящая три переменные, которые являются данным текущего числа.
  
## Самостоятельная работа №6
### В предложении ‘Hello World’ вставьте ‘my’ между двумя словами. Выведите полученное предложение в консоль в одну строку. Программа должна занимать не более двух строк редактора кода.

```python
a = 'Hello World'
print(a.replace(' ', ' my '))
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_6.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и `replace()`:

1. `print(a.replace(' ', ' my '))`: выводится строка Hello World с замененным пробелом на ' my '.

  
## Самостоятельная работа №7
### Узнайте длину предложения ‘Hello World’, результат выведите в консоль. Программа должна занимать не более двух строк редактора кода.

```python
a = 'Hello World'
print(len(a))
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_7.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и `len()`:

1. `print(len(a))`: Выводится количество символов строки переменной a.

  
## Самостоятельная работа №8
### Переведите предложение ‘HELLO WORLD’ в нижний регистр. Программа должна занимать не более двух строк редактора кода.

```python
a = 'HELLO WORLD'
print(a.lower())
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_8.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и `lower()`:

1. `print(a.lower())`: Выводится значение переменной а в нижнем регистре.

  
## Самостоятельная работа №9
### Самостоятельно придумайте задачу по проходимой теме и решите ее. Задача должна быть связанна со взаимодействием с числовыми значениями.
### Придуманная задача заключается в выводе результата умножения между двух чисел.

```python
a, b = 21, 5
print(a*b)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_9.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и математических функций:

1. `print(a*b)`: Выводится результат умножения переменной а на переменную b.

  
## Самостоятельная работа №10
### Самостоятельно придумайте задачу по проходимой теме и решите ее. Задача должна быть связанна со взаимодействием со строковыми значениями.
### Придуманная задача заключается в переведении строки 'hello world' в верхний регистр

```python
a = 'hello world'
print(a.upper())
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_2/pic/Sam2_10.png)

## Выводы

В данном коде выводятся строка с использованием функции `print()` и `upper()`:

1. `print(a.upper())`: Выводится значение переменной а в верхнем регистре.

## Общие выводы по теме
В ходе выполнения лабораторных и самостоятельных работ были использованы и изучены базовые возможности языка программирования python. Были рассмотрены возможности различных типов переменных и их различные функции, такие как `print()`, `input()` и т.д.
