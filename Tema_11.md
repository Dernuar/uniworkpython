# Тема 4. Функции и модули
Отчет по Теме #2 выполнил(а):
- Пинегин Дмитрий Александрович
- ПИЭ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | - |
| Задание 3 | + |
| Задание 4 | + |
| Задание 5 | + |
| Задание 6 |
| Задание 7 |
| Задание 8 |
| Задание 9 |
| Задание 10 |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Простой итератор, но у него нет гибкой настройки, например его нельзя развернуть. Он работает просто как next(), но нет prev()

```python
numbers = [0, 1, 2, 3, 4, 5]
for item in numbers:
    print(item)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Lab11_1.png)

## Выводы

В данном коде выводятся итерация списка numbers:

1. `numbers = [0, 1, 2, 3, 4, 5]`: Задаётся список'

2. `for item in numbers: print(item)`: выводятся значения списка через цикл


## Лабораторная работа №2
### Класс итератор с гибкой настройкой и удобными применением

```python
class countDown:
    def __init__(self, start):
        self.count = start + 1

    def __iter__(self):
        return self
    
    def __next__(self):
        self.count -= 1
        if self.count < 0:
            raise StopIteration
        return self.count
    
if __name__ == '__main__':
    counter = countDown(5)
    for i in counter:
        print(i)
```

### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Lab11_2.png)

## Выводы

В данном коде выводится итерация до необходимого числа:

1. `def __init__(self, start)`: инициализация счетчика

2. `def __iter__(self)`: возвращение объекта как итератора

3. `def __next__(self)`: выводит обратный расчёт 

4. `if __name__ == '__main__'`: выполнение функции напрямую через заданное число

## Лабораторная работа №3
### Генератор списка

```python
a = [i ** 2 for i in range(1, 5)]

print ('a - ', a)
for i in a:
    print(i)

print('iter(a) - ', iter(a))
for i in a:
    print(i)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Lab11_3.png)

## Выводы

В данном коде генерируются списки которые можно итерировать несколько раз:

1. `a = [i ** 2 for i in range(1, 5)]`: создание списка

2. `for i in a: print(i)`: вывод списка через итерацию

3. `print('iter(a) - ', iter(a))`: показывает объект итератора списка
  
## Лабораторная работа №4
### Выражения генераторы

```python
b = (i ** 2 for i in range(1,5))

print(b)
print('first')

for i in b:
    print(i)

print('second')

for i in b:
    print(i)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Lab11_4.png)

## Выводы

В данном коде генерируется список который можно итерировать только один раз:

1. `b = (i ** 2 for i in range(1,5))`: создание списка

2. `print(b)`: показывает другой вывод от генератора списков

3. `for i in b: print(i)`: вывод списка через итерацию

## Лабораторная работа №5
### Такой же счетчик, как и в первом задании, только это генератор и использует yield

```python
def countDown(count):
    while count >= 0:
        yield count
        count -= 1
    
if __name__ == '__main__':
    counter = countDown(5)
    for i in counter:
        print(i)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Lab11_5.png)

## Выводы

В данном коде итерируется список как и в первом задании, но с использованием yield:

1. `def countDown(count)`: задаётся функция в которой возвращается генератор с помощью yield.

2. `if __name__ == '__main__':`: выполнение функции напрямую через заданное число

3. `for i in counter: print(i)`: вывод списка через итерацию




## Самостоятельная работа №1
### Изучив новые возможности Python вы решили реализовать программу, которая считает числа Фибоначчи при помощи итераторов. Расчет начинается с чисел 1 и 1. Создайте функцию fib(n), генерирующую n чисел Фибоначчи с минимальными затратами ресурсов. Для реализации этой функции потребуется обратиться к инструкции yield (Она не сохраняет в оперативной памяти огромную последовательность, а дает возможность “доставать” промежуточные результаты по одному). Результатом решения задачи будет листинг кода и вывод в консоль с числом Фибоначчи от 200.

```python
def fib(n):
    a, b = 1, 1
    count = 0
    while count < n:
        yield a
        a, b = b, a + b
        count += 1

if __name__ == '__main__':
    fibo = fib(200)
    for i in fibo:
        result = i
    print(result)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_11/pic/Sam11_1.png)

## Выводы

В данном коде выводится определённое число фибоначчи

1. `def fib(n)`: задаётся функция в которой будет генератор для чисел фибоначчи

2. `while count < n`: цикл в котором будет использоваться формула для нахождения числа

3. `if __name__ == '__main__'`: вызов функции напрямую и последующий вывод в консоль необходимого числа


  

  
