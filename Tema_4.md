# Тема 4. Функции и модули
Отчет по Теме #2 выполнил(а):
- Пинегин Дмитрий Александрович
- ПИЭ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |
| Задание 7 | + |
| Задание 8 | + |
| Задание 9 | + |
| Задание 10 | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Напишите функцию, которая выполняет любые арифметические действия и выводит результат в консоль. Вызовите функцию используя “точку входа”.

```python
def main():
    print(2+2)


if __name__ == '__main__':
    main()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_1.png)

## Выводы

В данном коде выводятся строка с использованием функции и "точки входа":

1. `def main(): print(2+2)`: Задаётся функция 'main()'

2. `if __name__ == '__main__': main()`: Выполнения условия которые выполняет функцию main(), т.к. она запускается напрямую.


## Лабораторная работа №2
### Напишите функцию, которая выполняет любые арифметические действия, возвращает при помощи return значение в место, откуда вызывали функцию. Выведите результат в консоль. Вызовите функцию используя “точку входа”.

```python
def main():
    return 2 + 2

if __name__ == '__main__':
    print(main())
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_2.png)

## Выводы

В данном коде выводятся строка с использованием функции и "точки входа":

1. `def main(): return 2 + 2`: Задаётся функция 'main()'

2. `if __name__ == '__main__': print(main())`: Выполнения условия которые выполняет печатает вызванную функцию main(), т.к. она запускается напрямую.

## Лабораторная работа №3
### Напишите функцию, в которую передаются два аргумента, над ними производится арифметическое действие, результат возвращается туда, откуда эту функцию вызывали. Выведите результат в консоль. Вызовите функцию в любом небольшом цикле.

```python
def main(one, two):
    return one + two

for i in range(5):
    answer = main(one = 1, two = 10)
    print(answer)
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_3.png)

## Выводы

В данном коде выводятся строки с использованием функции и "точки входа":

1. `def main(one, two): return one + two`: Задаётся функция 'main()'

2. `for i in range(5):`: Задаётся небольщой цикл.

3. `answer = main(one = 1, two = 10) print(answer)`: Вызывается функция main() которая выполняет свою задачу сложения двух заданных чисел.
  
## Лабораторная работа №4
### Напишите функцию, на вход которой подается какое-то изначальное неизвестное количество аргументов, над которыми будет производится арифметические действия. Для выполнения задания необходимо использовать кортеж “*args”.

```python
def main(x, *args):
    one = x
    two = sum(args)
    three = float(len(args))

    print(f"one = {one}\ntwo = {two}\nthree = {three}")

    return x + sum(args) / float(len(args))

if __name__ == '__main__':
    result = main(5, 1, -3, 2, 4, 0)
    print(f'\nresult = {result}')
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_4.png)

## Выводы

В данном коде выводятся несколько строк с вызовом функции main():

1. `def main(x, *args):`: Задаётся функция main().

2. `if __name__ == '__main__':`: Условие которое выполняется при прямом вызове main().

3. `print(f'\nresult = {result}')`: Выводится результат программы.

## Лабораторная работа №5
### Напишите функцию, которая на вход получает кортеж “**kwargs” и при помощи цикла выводит значения, поступившие в функцию. Вызовите функцию используя “точку входа”.

```python
def main(**kwargs):
    for i in kwargs.items():
        print(i[0], i[1])

    print()

    for key in kwargs:
        print(f'{key} = {kwargs[key]}')

if __name__ == '__main__':
    main(x = [1, 2, 3], y = [3, 3, 0], z = [2, 3, 0], q = [3, 3, 0], w = [3, 3, 0])
    print()
    main(**{'x': [1, 2, 3], 'y': [3, 3, 0]})
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_5.png)

## Выводы

В данном коде выводятся несколько строк с вызовом функции main():

1. `def main(**kwargs):`: Задаётся функция main().

2. `for i in kwargs.items(): print(i[0], i[1])`: Задается небольшой цикл который будет выводить значения кортежа kwargs.

3. `for key in kwargs: print(f'{key} = {kwargs[key]}')`: Второй способ вывода значений кортежа.

4. `if __name__ == '__main__':`: Условие которое выполняется при прямом вызове main().

5. `main(x = [1, 2, 3], y = [3, 3, 0], z = [2, 3, 0], q = [3, 3, 0], w = [3, 3, 0])`: Задаётся значения кортежа.

6. `main(**{'x': [1, 2, 3], 'y': [3, 3, 0]})`: Задается значения кортежа вторым способом


## Лабораторная работа №6
### Напишите две функции. Первая – получает в виде параметра “**kwargs”. Вторая считает среднее арифметическое из значений первой функции. Вызовите первую функцию используя “точку входа” и минимум 4 аргумента.

```python
def main(**kwargs):

    for i, j in kwargs.items():
        print(f'{i}. Mean = {mean(j)}')

def mean(data):
    return sum(data) / float(len(data))

if __name__ == '__main__':
    main(x = [1, 2, 3, 4], y = [3, 3, 0, 2])
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_6.png)

## Выводы

В данном коде выводятся несколько строк с вызовом функции main() и mean():

1. `def main(**kwargs):`: Задаётся функция main().

2. `for i, j in kwargs.items(): print(f'{i}. Mean = {mean(j)}')`: Выводится значение используя функцию mean()

3. `def mean(data):`: Задаётся функция mean() которая высчитывает среднее арифмитеческое заданных чисел.

4. `if __name__ == '__main__': main(x = [1, 2, 3, 4], y = [3, 3, 0, 2])`: Выводится ср. арифмитическое заданных чисел, вызванной напрямую функцией main()

## Лабораторная работа №7
### Создайте дополнительный файл .py. Напишите в нем любую функцию, которая будет что угодно выводить в консоль, но не вызывайте ее в нем. Откройте файл main.py, импортируйте в него функцию из нового файла и при помощи “точки входа” вызовите эту функцию.

```python
def say_hello():
    print("Hello someone?")
```

```python
from for_import import say_hello

if __name__ == '__main__':
    say_hello()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_7.png)

## Выводы

В данном коде выводится строка с использованием функции из другого файла.

1. `def say_hello(): print("Hello someone?")`: Задается функция say_hello
2. `from for_import import say_hello`: Импортируется функция say_hello() из дополнительного файла
3. `if __name__ == '__main__': say_hello()`: Вызывается функция say_hello() с помощью точки входа

## Лабораторная работа №8
### Напишите программу, которая будет выводить корень, синус, косинус полученного от пользователя числа.

```python
import math

def main():
    value = int(input("Введите значение: "))
    print(math.sqrt(value))
    print(math.sin(value))
    print(math.cos(value))

if __name__ == '__main__':
    main()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_8.png)

## Выводы

В данном коде выводится строка с использованием функции main()из и модуля math().

1. `import math`: импортируются функции из модуля math.

2. `def main(): value = int(input("Введите значение: ")) print(math.sqrt(value)) print(math.sin(value)) print(math.cos(value))`: задается функция main() в которой задается аргумент числа и находится его корень, синус и косинус.

3. `if __name__ == '__main__': main()`: Вызывается напрямую функция main().


## Лабораторная работа №9
### Напишите программу, которая будет рассчитывать какой день недели будет через n-нное количество дней, которые укажет пользователь.

```python
from datetime import datetime as dt
from datetime import timedelta as td

def main():
    print(
        f'Today {dt.today().date()} '
        f'Day of the week - {dt.today().isoweekday()}'
    )
    n = int(input("Enter the amount of days: "))
    today = dt.today()
    result = today + td(days=n)
    print(
        f'After {n} days it will be {result.date()}. '
        f'Day of the week - {result.isoweekday()}'
    )

if __name__ == '__main__':
    main()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_9.png)

## Выводы

В данном коде выводится дата и день недели на данный момент и какой день недели будет через n-нное количество дней с помощью метода datetime() и main():

1. `from datetime import datetime as dt from datetime import timedelta as td`: импортируются функции datetime и timedelta из модуля datetime.

2. `def main():`: задается функция main()

3. Содержимое метода main выводит сегодняшнюю дату и день недели, задаётся число n, и с находится дата и день недели через n дней

4. `if __name__ == '__main__': main()`: задается условия вывода функции main() напрямую


## Лабораторная работа №10
### Напишите программу с использованием глобальных переменных, которая будет считать площадь треугольника или прямоугольника в зависимости от того, что выберет пользователь. Получение всей необходимой информации реализовать через input(), а подсчет площадей выполнить при помощи функций. Результатом программы будет число, равное площади, необходимой фигуры

```python
global result

def rectangle():
    a = float(input('Width: '))
    b = float(input('Height: '))
    global result
    result = a * b

def triangle():
    a = float(input('Base: '))
    h = float(input('Height: '))
    global result 
    result = 0.5 * a * h

figure = input('1 for rectangle, 2 for triangle: ')

if figure == '1':
    rectangle()
elif figure == '2':
    triangle()

print(f'Area: {result}')
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Lab4_10.png)

## Выводы

В данном коде выводятся площадь треугольника или прямоугольника (задается через консоль) используя функции triangle() и rectangle()

1. `global result`: задается глобальная переменная.

2. `def rectangle():`: задается функция rectangle() в которой черезь консоль задается ширина и высота фигуры, а затем высчитывается её площадь.

3. `def triangle():`: задается функция triangle() в которой черезь консоль задается основание и высота фигуры, а затем высчитывается её площадь.

4. `if figure == '1': rectangle() elif figure == '2': triangle()`: задается условие, определящее выбранную пользователем фигуру.

5. `print(f'Area: {result}')`: выводится результат площади




## Самостоятельная работа №1
### Дайте подробный комментарий для кода, написанного ниже. Комментарий нужен для каждой строчки кода, нужно описать что она делает. Не забудьте, что функции комментируются по-особенному.

```python
from datetime import datetime # импортирует datetime
from math import sqrt # импортирует sqrt

def main(**kwargs): # задается функция main()
    for key in kwargs.items(): # задается цикл
        result = sqrt(key[1][0] ** 2 + key[1][1] ** 2) # задается значение переменной result 
        print(result) # выводится переменная result

if __name__ == '__main__': # задается условие которое выполняется при вызове main() напрямую
    start_time = datetime.now() # задается переменная start_time которая является датой на момент запуска программы
    main( 
        one = [10, 3],
        two = [5, 4],
        three = [15, 13],
        four = [93, 53],
        five = [133, 15],
    ) # вызов функции main() в которой задается значения 5 спискам
    time_costs = datetime.now() - start_time # задается переменная time_costs которая является результатом вычитания из даты на данный момент переменной start_time
    print(f'Время выполнения программы: {time_costs}') # выводится значение time_costs являеющееся временем затраченным на выполнения программы
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Sam4_1.png)

## Выводы

Выводы кода представлены в комментариях самого кода
  
## Самостоятельная работа №2
### Напишите программу, которая будет заменять игральную кость с 6 гранями. Если значение равно 5 или 6, то в консоль выводится «Вы победили», если значения 3 или 4, то вы рекурсивно должны вызвать эту же функцию, если значение 1 или 2, то в консоль выводится «Вы проиграли». При этом каждый вызов функции необходимо выводить в консоль значение “кубика”. Для выполнения задания необходимо использовать стандартную библиотеку random. Программу нужно написать, используя одну функцию и “точку входа”

```python
from random import randint

def main():
    dice = randint(1, 6)
    print(f"Die result: {dice}")

    if 5 <= dice <= 6:
        print("You win!")
    elif 3 <= dice <= 4:
        main()
    else:
        print("You lose.")
    

if __name__ == '__main__':
    main()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Sam4_2.png)

## Выводы

В данном коде выводятся значения выпавшего кубика с помощью функции main():

1. `from random import randint`: импортируется функция из модуля random.

2. `def main():`: Задается функция main() в которой задается случайное число от 1 до 6 в виде переменной dice, после этого выводится результат проигрыша, выигрыша или переброс кубика в зависимости от выпавшего значения.

3. `if __name__ == '__main__': main()`: вызывается функция main() напрямую
  
## Самостоятельная работа №3
### Напишите программу, которая будет выводить текущее время, с точностью до секунд на протяжении 5 секунд. Программу нужно написать с использованием цикла. Подсказка: необходимо использовать модуль datetime и time, а также вам необходимо как-то “усыплять” программу на 1 секунду.

```python
from time import sleep
from datetime import *

def main():
    for i in range(5):
        print(datetime.now().strftime('%H:%M:%S'))
        sleep(1)

if __name__ == '__main__':
    main()
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Sam4_3.png)

## Выводы

В данном коде выводятся текущее время с точностью до секунд на протяжении 5 секунд используя модули datetime и time:

1. `from time import sleep from datetime import *`: импортируются функции из datetime и time.

2. `def main():`: Задается функция main() в которой в цикле выводится текущее время и делается пауза программы на одну секундну с помощью time.sleep().

3. `if __name__ == '__main__': main()`: вызывается функция main() напрямую 
  
## Самостоятельная работа №4
### Напишите программу, которая считает среднее арифметическое от аргументов вызываемое функции, с условием того, что изначальное количество этих аргументов неизвестно. Программу необходимо реализовать используя одну функцию и “точку входа”.

```python
def main(x, *args):
    amount = x
    return sum(args)

if __name__ == '__main__':
    result = main(3, 2, 1, 3)
    print(f'Summary of numbers: {result}')
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Sam4_4.png)

## Выводы

В данном коде выводятся среднее арифметическое от аргументов вызываемое функцией main():

1. `def main(x, *args):`: задается функция в которой находится среднее арифметическое.

2. `if __name__ == '__main__': result = main(3, 2, 1, 3) print(f'Summary of numbers: {result}')`: выводится ср. знач. от заданных аргументов через main() напрямую


## Самостоятельная работа №5
### Создайте два Python файла, в одном будет выполняться вычисление площади треугольника при помощи формулы Герона (необходимо реализовать через функцию), а во втором будет происходить взаимодействие с пользователем (получение всей необходимой информации и вывод результатов). Напишите эту программу и выведите в консоль полученную площадь.

```python
import math

def Hero(a, b, c):
    p = (a + b + c)/2
    return math.sqrt(p * (p - a) * (p - b) * (p - c))
```
```python
from Sam4_52 import Hero

if __name__ == '__main__':
    result = Hero(20, 35, 54)
    print(f"Area of triangle: {result}")
```
### Результат.
![Меню](https://github.com/Dernuar/uniworkpython/blob/Тема_4/pic/Sam4_5.png)

## Выводы

В данном коде выводятси площадь треугольника используя два файла:

1. В первом файле создаётся функция Hero в которой высчитывается площадь треугольника используя формулу Герона.

2. Во втором файле импортируется функция из первого файла и вызывается напрямую
  
